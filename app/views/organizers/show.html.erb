

<%= render './partials/header'  %>
<div class="container">
	<h1>Welcome <%= current_user.first_name  %></h1>

		<% @events.each do |event| %>
			<ul>
				<li><%= event.name  %></li>
			</ul>
		<% end %>

	<a href="<%= organizers_addevent_path  %>">Add New Event</a>
</div>
